hosts    = {{ .DBHOST }}:{{ .DBPORT }}
user     = {{ .DBUSER }}
password = {{ .DBPASS }}
dbname   = {{ .DBNAME }}

option_group = client

query = SELECT goto FROM alias WHERE address='%s' AND active = 1
